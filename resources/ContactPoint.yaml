$fn: ["telecom"]
$body: 
  $map: $ telecom 
  $as: i
  $body: 
    system: $ get(i, "system") 
    value:
      $if: $ get(i, "system") = "email"
      $then: $ fns.randEmail()
      $else: $ fns.randPhone()
