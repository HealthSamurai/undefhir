randHumanName:
  $fn: ["name"]
  $body:
    $map: "$ name"
    $body: 
      family: "$ randNth (dictionary.animals)" 
      given:
        $map: "$ name.given"
        $body:
          "$ randNth (dictionary.colors)"
        
